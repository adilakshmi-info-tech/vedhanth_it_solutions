node_modules
.next
.git
.env
.env.local
npm-debug.log*

# docker-compose bind mounts on the deploy VM (postgres data owned by the
# container's internal uid — unreadable by the host user, must never be
# swept into the build context)
data
